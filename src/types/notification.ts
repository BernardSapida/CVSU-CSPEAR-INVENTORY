interface UserNotification {
    id: string,
    isViewed: boolean,
    createdAt: Date
    borrowRequestId: string,
}

interface AdminNotification {
    id: string,
    isViewed: boolean,
    createdAt: Date
    borrowRequestId: string,
}